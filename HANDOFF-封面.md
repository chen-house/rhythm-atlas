# 节奏图鉴 · 封面 交接文档

新会话从这份文档起步，不用回读旧对话。

---

## 1. 要做的东西

`leaning/作品集/rhythm-site/` 这个站的首页封面。一片林子，五棵树＝五个节奏类型，一只鸟。

**封面要达到的效果，只有四条：**

1. 好看、记得住——像专辑封面（棱镜、斑马线），**不解释内容**
2. 一眼知道有哪些流派——五棵树、树下五个名字
3. 点得进去——每棵树是入口，链到对应流派页
4. 有个活物，让人想多待一会儿——一只鸟

**只有第 2 条承担信息，其余全是画。**

---

## 2. 已定案（含否掉的，别再走回头路）

| 决策 | 结论 | 理由 |
|---|---|---|
| 水面三层构图 | **否** | 水只是背景可有可无，却被做成结构层吃掉 41% 画面 |
| 根系表达关系 | **否** | 画得像根就读不出关系，读得出关系就不像根——自相矛盾，不是难度问题 |
| 树的长相表达节奏性格 | **否** | 盲配 1/5，实测失败。形容词是为了"能画成树"倒推的，不是听感 |
| 果实＝代表曲目 | **否** | 曲目是进了流派页才有的内容，封面不需要第二层导航颗粒度 |
| 鸟逐棵飞、穿针引线表达关系 | **否** | 观众只看到鸟在换地方；"访问顺序"不等于"血缘"。传不出去 |
| 冠交叠 | **留，但不承担信息** | 只为让五棵像一片林子，不是并排图标 |
| 关系这层 | **只保留两样** | 梢色继承上游 + 左右顺序＝年代顺序。别再加东西 |
| 参考 | 霍克尼《沃尔特附近的大树》(2007) | 裸枝、线细而清晰、颜色出在干/地/天不出在叶 |
| 风格 | 裸枝、不画冬天 | 暖米底 + 每棵树自己的干色 + 地面带 + 高饱和的鸟 |

**反复踩的坑（写死）：给画面上每个元素都派一个信息任务。根表关系、鸟表风格、果表曲目——结果画面被塞满，反而什么都读不出。**

---

## 3. 已验证的数据（实测，别再重验）

**线宽与密度**：主干起 8–12px，每级 ×0.66–0.78，细到 0.42px 截断。递归深度 **8–9**（256–512 个梢）是甜点，10 开始结块。1x 下每根线清晰，不糊。

**树的画法**：干和冠必须解耦——主干单独长（不消耗冠的递归深度），冠从干顶起分叉。一段枝切成小段做连续收细，小段用 round 端点（butt 会露白缝）。冠的**前四层是骨架、定死不随机**，只让细梢随机，否则换个种子冠宽能摆 37%。低位大侧枝的方向也必须定死。

**体量归一**：只缩枝长（迭代调 len0 到目标高度），**不要整体 scale**，否则矮树放大后干变成粗柱子。

**"重心偏移"不等于"压扁一侧"**：曾把 asym 写成"放大一侧分叉角"，实际把另一侧删没了，出来是半棵树。正确做法是整个冠朝一个方向有界偏转（pullTo/pull），两侧照常分叉。

**鸟的剪影**：参数级差异（长宽比、旋转）在 60px 传不到眼睛；**拓扑级差异（胖圆无颈／弯月／长颈长腿／楔形／小身长尾／冠羽）在 60px 能分辨**。

**落枝动画有生命感的必要条件**：弧线飞行 / 落前减速抬胸伸腿（预备）/ 触枝压扁 / 回弹 / 尾比身体晚一拍、头再晚一点 / 落定两三下衰减余震。

**动画的验证方法**（比截图有效）：把姿态计算抽成 `poseAt(t)` 单独可调用，然后①在每个相位交界前后各取一帧比差值，查跳变；②全程每 5ms 采样查 x 有没有回退（倒着飞）。两个真 bug 就是这么抓到的，静态截图看不出来。

---

## 4. 五棵树的参数（当前值，标准一全部达标）

顺序＝画面从左到右＝年代顺序：`blues → gospel → jazz → funk → hiphop`
x 位置 `[240, 435, 610, 790, 1035]`，种子 `[11,29,47,83,101]`，统一树高 330，地面线 y=520，画布 1200×640。

梢色继承：第 i 棵的 tip 色 = mix(自身 tip, 上一棵的 wood, 0.28)。

完整参数在 `rhythm-cover-gen.js` 的 `BASE` 对象里，直接看代码，别手抄。

---

## 5. 当前文件

| 文件 | 是什么 |
|---|---|
| `rhythm-cover-gen.js` | 生成器（Node），改参数改这个 |
| `rhythm-cover.html` | 生成结果，可直接打开播放 |
| `_cover-v1.html` / `_cover-gen.js` | 旧的水面+根系版，**已废弃**，可删 |
| `_concept-v2.html` | 更早的概念稿，已废弃 |
| `index.html` | 站点首页，**封面还没接进去** |

当前档位：**可用**（跑通、无报错、能播、能点）。不是可发布。

---

## 6. 待办

### A. 发布前必改（三条）

1. **鸟不再逐棵巡逻**。改成：飞进来落一棵就住下；隔几十秒随机换一棵，或原地梳毛、歪头。现在每 1.5 秒换一棵、五棵挨个走完，像程序在遍历列表。
2. **树的反应改成树梢轻晃**。现在是整个树组位移＋旋转，是地震不是落鸟。只让落点周围几根细枝晃，幅度小到要盯着才看见，两三下停。
3. **拆开右边两棵**。Funk 的冠伸进 Hip-hop 地盘，糊成一棵。

### B. 继续优化（记录，暂不做）

- Jazz 下方有一小团侧枝跟主冠断开，悬在半空
- Gospel 的冠偏"扫帚"
- 鸟站定后轮廓不够松弛，缺一下"收拢"
- 鸟只有正侧面，不会转身（所以只能从左往右飞）
- 五棵树近看是同一个分形质感，差异只在整体姿态
- 窄屏没适配
- rAF 跑完 21 秒停死
- 封面没接进 `index.html`

---

## 7. 协作方式

**四档交付**：跑通 → 可用 → 可发布 → 继续优化（记录不做）。**报到档位，不要每步找人验证。**

**每轮只报三件事**：改了什么 / 验了什么 / 还不确定什么。不宣布"搞定"。

**卡两轮就停**：同一处改两轮还不对 = 方法错了，报告换法子。

**做不了要先说**：动手前自检两问——能不能做出来、能不能验证做对了。任一为否，立刻说，并带出路。

**其他**：一次只改一个变量并说明旧→新；视觉改动必须先能看到渲染结果再改，且要按**真实显示尺寸**判断（曾在 2400px 放大图上判断"能分辨"，实际 1200px 下不成立）。
